<% if @test %>
    var primary_domain=window.location.hostname.split(".").splice(-2,2).join(".");
    console.log("I'm being called in erb")
    <%= puts "I'm being invoked inside erb" %>
    <% if cookies[:id_rb] %>
        document.cookie = 'id_js_erb=<%= cookies[:id_rb] %>; domain='+primary_domain+';path=/; expires=<%= (Time.zone.now + 30.days).utc.strftime("%a, %d %b %Y %H:%M:%S UTC") %>';
    <% end %>
<% end %>