<!DOCTYPE html>
<html>
  <div class="radial-gradient", style="height:100%">
  <head>
  <div class="topnav", width: 100%>
    <a class="active"<%= link_to "Adarsh's Blog", adarsh_path, class: "btn btn-success btn-lg" %></a>
    <%= link_to "Home", root_path, class: "btn btn-success btn-lg" %>
    <%= link_to "Bloggers", users_path, class: "btn btn-success btn-lg" %>
    <%= link_to "About", about_path, class: "btn btn-success btn-lg" %>
    <% if logged_in? %>
      <%= link_to "Create article", new_article_path, class: "btn btn-success btn-lg" %>
      <%= link_to "Create category", new_category_path, class: "btn btn-success btn-lg" if current_user.admin?%>
      <%= link_to current_user.username, user_path(current_user),  class: "btn btn-success btn-lg" %>
      <%= link_to "Logout", logout_path, data: {'turbo-method': :delete },  class: "btn btn-success btn-lg" %>
      <p style="color: white"><%= "(Admin)" if current_user.admin?%></p>
    <% else %>
      <%= link_to "Login", login_path, class: "btn btn-success btn-lg" %>
      <%= link_to "Sign up!", signup_path, class: "btn btn-success btn-lg" %>
    <% end %>
  </div>
      <title>Blog</title>
    <%
=begin%>
 <%= link_to 'Set Cookie', cookies_set_cookie_path %>
    <%= link_to 'Set Cookie', cookies_set_cookie_path, id: 'set-cookie-link' %> 
<%
=end%>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <%= render 'layouts/flash' %>
    <%= yield %>
  </body>
 <%= javascript_include_tag 'set_cookie' %> 
 </div>
</html>

<style>

body {
   margin: 0;
   padding: 0;
   background: 
    radial-gradient(
     at center, #3498db, #9b59b6
    );
}
/* Add a black background color to the top navigation */
.topnav {
  background-color: #333;
  overflow: hidden;
}

/* Style the links inside the navigation bar */
.topnav a {
  float: left;
  color: #f2f2f2;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
  font-size: 17px;
}

/* Change the color of links on hover */
.topnav a:hover {
  background-color: #ddd;
  color: black;
}

/* Add a color to the active/current link */
.topnav a.active {
  background-color: #04AA6D;
  color: white;
}

.radial-gradient {
  position:relative;
  top:0px;
  left:0px;
  height:100%;
  width:100%;
  
  /*Fallback if gradeints don't work */
  background: #9b59b6;
  /*Linear gradient... */
  background: 
    radial-gradient(
     at center, #3498db, #9b59b6
    );
}

.headings{
  padding:20px;
  color: black; 
  background: whitesmoke; 
  border: 1px solid black; 
  border-radius:10px; 
  width: 10%
}
.smallheadings{
  padding:10px; 
  border: 1px solid black; 
  border-radius: 10px; 
  width: 150px; 
  background: whitesmoke;
}
.imgborder{
  box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19); width:80px;
}
</style>
